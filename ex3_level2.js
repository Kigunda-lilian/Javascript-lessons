let base = parsefloat(prompt("Enter base"));
let height = parsefloat(prompt("Enter the height"));
let area1 = 0.5 * base * height;
// let area = alert(`The area of the triangle is ${base}*${height}*0.5`); //not correct
let totalArea = alert(`The total area is ${area1}`)
console.log("The area of the triangle is:" + area1+"square units")